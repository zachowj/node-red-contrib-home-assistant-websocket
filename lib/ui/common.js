// eslint-disable-next-line no-unused-vars, no-var
var ha = (function () {
    const nodeColors = {
        action: '#46B1EF',
        alpha: '#E78BB9',
        api: '#7CDFFD',
        beta: '#77DD77',
        data: '#5BCBF7',
        event: '#399CDF',
    };

    return {
        nodeColors,
    };
})();
