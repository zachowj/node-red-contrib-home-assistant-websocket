export enum IdSelectorType {
    Floor = 'floor',
    Area = 'area',
    Device = 'device',
    Entity = 'entity',
    Label = 'label',
    Substring = 'substring',
    Regex = 'regex',
}

export enum PropertySelectorType {
    Floor = 'floorRegistry',
    Area = 'areaRegistry',
    Device = 'deviceRegistry',
    State = 'stateObject',
    Label = 'labelRegistry',
}
